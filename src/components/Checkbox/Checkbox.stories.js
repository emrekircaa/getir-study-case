import { Checkbox } from ".";

const meta = {
  title: "Components/Checkbox",
  component: Checkbox,
  parameters: {
    layout: "centered",
  },
  tags: ["autodocs"],
};

export default meta;

export const Checked = {
  args: {
    label: "Checkbox",
    isChecked: true,
    count: 1,
  },
};
